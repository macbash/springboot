name: my workflow
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set env
      run: if [ $? -eq 0 ]; then touch RESULT && exit 1; fi
    - name: file creation
      uses: actions/upload-artifact@v2
      if: always()
      with:
        name: file
        path: |
          RESULT
      
